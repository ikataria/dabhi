const dbPost = require('../../../model/post/post')
const dbProfile = require('../../../model/registration/profile')

/**
 * find check if two are connected to each other
 * if yes, then show one's post to another
 */

module.exports = (req, res) => {

}